from django.apps import AppConfig

class ResultadosConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "webapp_django.apps.resultados"
    verbose_name = "Resultados"
