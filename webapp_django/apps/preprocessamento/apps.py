from django.apps import AppConfig

class PreprocessamentoConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "webapp_django.apps.preprocessamento"
    verbose_name = "Pré-processamento"
