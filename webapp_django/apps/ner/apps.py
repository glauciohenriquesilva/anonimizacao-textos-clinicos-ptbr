from django.apps import AppConfig

class NerConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "webapp_django.apps.ner"
    verbose_name = "NER — Reconhecimento de Entidades"
