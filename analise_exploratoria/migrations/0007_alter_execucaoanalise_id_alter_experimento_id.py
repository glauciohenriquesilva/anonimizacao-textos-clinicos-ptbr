# Este arquivo foi gerado incidentalmente por um `makemigrations` disparado a partir de
# outro app (preprocessamento) em um ambiente com uma versão de Django ligeiramente
# diferente da usada no projeto (detectou um AlterField de id sem relação com nenhuma
# mudança real de modelo). Neutralizado para não alterar nada — sem operações.
# Seguro de apagar manualmente se preferir; mantido apenas para não deixar um número de
# migration "furado" na sequência.

from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ('analise_exploratoria', '0006_experimento_execucaoanalise_experimento'),
    ]

    operations = []
