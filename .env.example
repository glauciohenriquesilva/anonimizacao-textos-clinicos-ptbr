# Copiar para .env e preencher com os valores reais
# NUNCA commitar o .env no git

# Django
SECRET_KEY=sua-chave-secreta-aqui
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL
DB_NAME=anonimizacao_clinica
DB_USER=postgres
DB_PASSWORD=sua-senha-aqui
DB_HOST=localhost
DB_PORT=5432

# Paths dos dados (absolutos ou relativos ao projeto)
DATA_RAW_PATH=data/raw/
DATA_PROCESSED_PATH=data/processed/
DATA_ANNOTATED_PATH=data/annotated/
OUTPUTS_PATH=outputs/

# HuggingFace (opcional — para download automático de modelos)
# HF_TOKEN=seu-token-aqui
